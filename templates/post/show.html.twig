{% extends 'base.html.twig' %}
{% block stylesheets %}
   {{  parent() }} 
{% endblock %}
{% block title %}Article de Reference: {{ post.reference }}{% endblock %}
    {% block body %}
    <div class="container">
        <h1>Données de l'artcle N° {{ post.id}}</h1>
          <div class="card" style="width:18rem;">
            <img src="{{ asset('images/'~post.image )}}" class="card-img-top" alt="...">
            <div class="card-body">
              <h5 class="card-title">{{ post.title}} de: {{post.author}}</h5>
              <h6 class="card-subtitle mb-2 text-muted ">Age: {{post.publishedAt|date('d/m/Y')}}</h6>
              <p class="card-text">{{ post.content}}</p>
            
            <p><a href="{{path('app_person')}}">Accueil</a></p>
            </div>
          </div>
        </table>
    </div>
    {% endblock %}
    